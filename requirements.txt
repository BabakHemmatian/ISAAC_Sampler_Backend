# ASGI app + framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.24,<1.0

# Pydantic (EmailStr needs the extra dependency below)
pydantic>=2.5,<3.0
email-validator>=2.0,<3.0

# Data stack
pandas>=2.1,<3.0
numpy>=1.23,<3.0
pyarrow>=14,<20

# S3 / storage
boto3>=1.28,<2.0
s3fs>=2024.2.0

# DB + SMTP
asyncpg>=0.28,<1.0
aiosmtplib>=2.0,<3.0

# Env config
python-dotenv>=1.0,<2.0
